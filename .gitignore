# Byte-compiled / optimized
__pycache__/
*.py[cod]
*$py.class
*.so

# Packaging / build
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# Environments
.venv/
venv/
env/
.python-version

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.idea/
.vscode/

# NOTE: outputs/*.png are intentionally TRACKED so the README gallery renders
# on GitHub. They are regenerated by the demos and by CI. If you would rather
# generate them on the fly, ignore them here and drop the gallery <img> tags.
